

## Plan: Export Wax Seal in Chocolate Brown

Create a standalone SVG file (`public/wax-seal-brown.svg`) replicating the current wax seal but using the chocolate brown color (`hsl(30, 16%, 36%)` → approximately `#8C7A6B`) instead of Tag Red (`#C54A3D`).

### File to create
- **`public/wax-seal-brown.svg`** — Same structure as `favicon.svg` but with the main circle fill changed to `#8C7A6B` (chocolate brown).

